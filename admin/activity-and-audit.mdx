---
title: "Activity and audit"
description: "See what AI assistants did, export company-wide logs on Enterprise, and stream to your SIEM."
---

Passport keeps two kinds of records:

- **Tool-call activity** — what AI assistants did through Passport (every plan; you always see your own)
- **Privileged admin audit** — admin changes such as approvals, role edits, and guardrail updates (Enterprise)

Company-wide export and SIEM streaming are Enterprise controls.

In the product, the member nav label is **Activity** (page title **Your activity**). On Enterprise, the company-wide tool-call log lives under the nav label **Audit**. Privileged admin actions are also available from **Settings → Admin activity**.

## Activity

Every tool call through Passport lands in the activity log. Each row shows who did it, their team, the app, the tool, which AI assistant was used, the outcome (ok, error, or blocked), how long it took, and a short summary.

On Free and Pro, each person sees their own Activity. On Enterprise, admins can browse the whole workspace from **Audit**, filter it, and export it.

Passport CLI calls can show an optional **via CLI** label (self-reported) so you can tell Codex apart from Claude Code in the log. The signed-in person or agent, and the AI client policy, remain authoritative.

A **blocked** outcome means a guardrail or policy stopped the call — not a random error.

### Filters and deep links

Enterprise admins can filter by person, app, and outcome. Filters live in the URL, so you can share a view. Guardrail alerts can deep-link into a matching filtered view.

## Audit

The audit log is an **Enterprise** trail of privileged actions: approving or removing an MCP, changing a person's role, creating a guardrail, generating a SCIM token, and so on. Each entry records the actor, the action, the target, and a detail such as the old and new value. Free and Pro do not expose this workspace audit API or CSV.

## Export

<CardGroup cols={2}>
  <Card title="Enterprise activity export" icon="table">
    `GET /export/activity.csv` and `GET /export/activity.jsonl` require Enterprise workspace controls. Both accept the same `member`, `app`, `outcome`, and `department` filters as the activity view (`department` is the team name). The JSONL export is SIEM-shaped, one JSON object per line.
  </Card>
  <Card title="Enterprise audit export" icon="file-lines">
    `GET /export/audit.csv` exports the privileged-action trail and also requires Enterprise.
  </Card>
</CardGroup>

Every plan can download a full workspace backup at `GET /export/workspace.json` (admin-only). That backup is for restore and support, not a substitute for the SIEM-shaped activity stream.

<Note>
  Secrets never reach these exports. Anything a guardrail would flag is scrubbed from the activity entry before it is persisted, so a blocked credential cannot leak through the CSV or JSONL you share in an incident review. See [Guardrails](/admin/guardrails).
</Note>

## Stream to your SIEM

On the Enterprise plan you can stream both logs to your SIEM over HTTP and to an OpenTelemetry collector, per workspace. See [SIEM and OTLP export](/enterprise/siem-export).
