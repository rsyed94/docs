# Passport docs

Public Mintlify source for [docs.passportmcp.com](https://docs.passportmcp.com).

Canonical edits live in the private `mcp-passport` repository under `docs/`. Sync **customer MDX**, `docs.json`, assets, and `.mintignore` only. Do not copy monorepo-only `*.md` archives (`DEPLOY.md`, `OPERATIONS.md`, competitive audits, product-spec archives, and similar).

Mintlify deployment: subdomain `passport`, branch `main`.
