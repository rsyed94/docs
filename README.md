# Passport docs

Public Mintlify source for [docs.passportmcp.com](https://docs.passportmcp.com).

Canonical edits live in the private `mcp-passport` repository under `docs/`. Sync **customer MDX**, `docs.json`, assets, and `.mintignore` only. Do not copy monorepo-only `*.md` archives.

Mintlify deployment: subdomain `passport`, GitHub repo `rsyed94/docs`, branch `main`. Keep this repository named `docs` so Mintlify's Git Source binding keeps working.
